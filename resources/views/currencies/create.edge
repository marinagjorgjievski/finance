@layout('layout.default')

@section('content')

<article class="br3 ba b--black-10 mv4 w-100 w-50-m w-25-l mw6 shadow-5 center">
  <main class="pa4 black-80">
    <div class="measure">
      <form action="/currencies" method="POST">
        {{ csrfField() }}
        <fieldset id="" class="ba b--transparent ph0 mh0">
          <hr>
          <legend class="f1 fw6 ph0 mh0">Create Currency</legend>
          <div class="mt3">
            <label class="db fw6 lh-copy f6" For="name">Name</label>
            <input class="pa2 input-reset ba bg-transparent hover-bg-black hover-gray w-100" type="text" name="name"  id="name" value="{{ old('name', '')}}">{{elIf('<span class = "text-danger">$self</span>', getErrorFor('name'), hasErrorFor('name')) }}
          </div>
          <div class="mt3">
            <label class="db fw6 lh-copy f6" For="symbol">Symbol</label>
            <input class="pa2 input-reset ba bg-transparent hover-bg-black hover-gray w-100" type="text" name="symbol"  id="symbol" value="{{ old('symbol', '')}}">{{elIf('<span class = "text-danger">$self</span>', getErrorFor('symbol'), hasErrorFor('symbol')) }}
          </div>
          {{elIf('<span class = "text-danger">$self</span>', getErrorFor('symbol'), hasErrorFor('symbol')) }}
        </fieldset>
        <div class="btn-position">
          <button type="submit" class="btn btn-secondary">Add</button>
          <a href="/currencies" role="button" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </main>
</article>

@endsection

