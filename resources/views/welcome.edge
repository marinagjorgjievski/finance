@layout('layout/default')

@section('content')
<div class="container">
  {{ style('css/style-homepage') }}
  {{ style('css/style') }}
  <h1>Create your accounts
    @if(auth.user)
      {{ auth.user.username }}
    @endif
  </h1>
</div>
@endsection
